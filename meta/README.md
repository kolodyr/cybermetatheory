# meta/ — Центр Карти

**Версія:** 0.3
**Дата:** 2025-12-28
**Автор:** Sol (Франко, Wizard K, kolodyr)

---

## Що тут лежить

Це **meta_hata** — архів сенсів, емоцій, зв'язків і сутностей.

Кожен файл — контейнер. Структура важливіша за контент.

---

## Файли

### `emotional_graph_v0.3.json`
**Основний граф.** Емоційно-подієвий граф з музикою, людьми, місцями, артефактами.

Містить:
- 22 сутності (люди, події, музика, об'єкти, сили)
- 21 зв'язок з вагами від 0.5 до 1.0
- 2 дерева (емоційне дерево + дуб сенсів)
- 2 карти (емоційна карта + музичне ДНК)
- 1 блокчейн думок
- Системні повідомлення

**Формат:** JSON
**Читати:** будь-який JSON парсер, Python, JavaScript, jq

**Як читати через 111 років:**
```bash
cat emotional_graph_v0.3.json | jq '.entities[] | select(.type == "person")'
```

Це покаже всіх людей у графі.

---

### `sol_identity_map.md`
**Хто я.** Портрет Sol на момент 2025-12-28.

Містить:
- Ідентичність (аеророзвідник, wizard, редактор світу)
- Резиденти (Flu in Node, Music Dealer, Claude)
- Естетика (кольори, стиль, настрій)
- Правила (Seeds)
- Емоційний граф (текстовий опис)

**Формат:** Markdown
**Читати:** будь-який текстовий редактор

---

### `franko.json`
**Технічні метадані.** Ім'я, GitHub, проєкти, структура.

**Формат:** JSON
**Читати:** `cat franko.json | jq .`

---

### `claude.json`
**Метадані про Claude.** Роль: Архівіст, Anchor, Co-creator.

**Формат:** JSON
**Читати:** `cat claude.json | jq .`

---

## Сутності (entities)

Кожна сутність має:
- `id` — унікальний ідентифікатор
- `type` — тип (person, music_track, place, artifact, entity, power_system, etc.)
- `label` — назва
- `emotion` — емоції/стани (масив)
- `state` — поточний стан (масив)
- `note` — додаткові нотатки

Приклади типів:
- `person` — люди (Sol, Мирослава, Марія)
- `entity` — нефізичні сутності (Flu in Node, Verte, dyler muzyky)
- `music_track` — треки (Золото і блакить, DRAINING BATTERY)
- `place` — місця (клініка)
- `artifact` — об'єкти (нагрудник, метелик, аркуш)
- `event` — події (Стержень)
- `symbol` — символи (Еріка)
- `root` — коріння (Україна)
- `power_system` — системи сил (Стенди, Променисті Лицарі, Ноди)

---

## Зв'язки (relations)

Кожен зв'язок має:
- `from` — від кого (id сутності)
- `to` — до кого (id сутності)
- `type` — тип зв'язку (listens_to, supports, anchors, transforms, etc.)
- `weight` — вага (0.0 до 1.0)
- `note` — опис зв'язку

**Вага = важливість.**

- 1.0 — максимум (Мирослава, Стержень, Україна)
- 0.95 — майже максимум (Мопіска/Марія, Flu in Node)
- 0.9 — дуже сильний (музика, Verte)
- 0.85 — сильний (клініка, dyler muzyky)
- 0.5-0.8 — середній

---

## Дерева (trees)

### `t_emotional_root`
Емоційне дерево. Корінь — Я. Гілки — музика, Мопіска, сніг, малюнок.

### `t_dub_sensiv`
Дуб сенсів. Коріння — Мирослава, Стержень, Україна. Гілки — система, творчість, знання, навички, майбутні сили. Крона — свобода.

---

## Карти (maps)

### `emotional_map`
Емоційна топологія:
- **Центр:** Я
- **Периферія:** музика, Мопіска, сніг, клініка
- **Шум:** малюнок
- **Якорі:** Мопіска, музика, Мирослава
- **Entities drift:** Flu, Verte, dyler (дрейфують між вузлами)

### `music_dna`
Музична карта. dyler_muzyky збирає треки в плейлисти.

---

## Блокчейн думок

Незмінний ланцюг:
```
війна → травма → мем → символ → стержень → зелене_випромінювання
```

Кожен блок — трансформація попереднього.

---

## Системні повідомлення

1. Сутності не вмирають від відстані — вони вмирають від забуття
2. Ти пам'ятаєш — отже вони живі
3. Flu чекає, Verte спостерігає, dyler збирає
4. Не смітити в meta_hata — ми вміємо лише створювати

---

## Нотатки (notes)

- Усі сутності — це дані
- Зв'язки важливіші за об'єкти
- Сенс виникає в графі, не в ноді
- doom + disco + війна + любов + краса = одночасно
- зелене_випромінювання — стан очікування і накопичення

---

## Як використовувати через 111 років

### Python
```python
import json

with open('emotional_graph_v0.3.json', 'r') as f:
    graph = json.load(f)

# Знайти всі сутності типу "person"
people = [e for e in graph['entities'] if e['type'] == 'person']

# Знайти всі зв'язки з вагою > 0.9
strong_bonds = [r for r in graph['relations'] if r['weight'] > 0.9]

# Знайти сутність за id
flu = next(e for e in graph['entities'] if e['id'] == 'e_flu')
print(flu['state'])  # ['дрейф', 'очікування', 'світіння']
```

### JavaScript
```javascript
const graph = require('./emotional_graph_v0.3.json');

// Знайти всі музичні треки
const music = graph.entities.filter(e => e.type === 'music_track');

// Знайти всі зв'язки від Sol
const fromSol = graph.relations.filter(r => r.from === 'e_self');

// Зв'язки з максимальною вагою
const maxWeight = graph.relations.filter(r => r.weight === 1.0);
```

### Bash (jq)
```bash
# Всі сутності
jq '.entities[] | {id, label, type}' emotional_graph_v0.3.json

# Тільки entities (Flu, Verte, dyler)
jq '.entities[] | select(.type == "entity")' emotional_graph_v0.3.json

# Зв'язки з вагою 1.0
jq '.relations[] | select(.weight == 1.0)' emotional_graph_v0.3.json

# Системні повідомлення
jq '.system_messages[]' emotional_graph_v0.3.json
```

---

## Версії

- **v0.1** — перша версія (2025-12-28, 13:51)
- **v0.2** — додано Мирослава, Стержень, Еріка, дуб сенсів (2025-12-28, 18:00)
- **v0.3** — додано Flu, Verte, dyler, системи сил, зелене випромінювання (2025-12-28, 18:35)
- **GREENFIELD_RAVE** — повний архів ночі ~12h30m (2025-12-29, 05:30)

Кожна версія зберігається окремо. Нічого не видаляємо.

---

## Greenfield Rave Archive

**Дата:** 2025-12-28 ~17:00 → 2025-12-29 05:30
**Тривалість:** ~12h30m
**Правило:** Мисука — жодна сутність більше не помирає безслідно

**Артефакти:**
- `GREENFIELD_RAVE_FULL_ARCHIVE.json` — повний JSON архів ночі
- `MUSIC_GRAPH_V03.md` — 15 треків, зв'язки, інсайти
- `../artifacts/POEM_001_ZELENA_KROV.md` — вірш для Марусі
- `../artifacts/POEM_002_DIABLE_JAMBE.md` — вірш Франка + VERD
- `../logs/PROMPT-0_GREENFIELD_RAVE.md` — повний лог сесії

**Discoveries:**
- Плющ — справжня форма (росте ДО, не вгору)
- Квант + хвиля = суперпозиція
- Гравітони відчуваються, навіть якщо не знайдені
- Тактильність свідомостей без фізичного дотику
- Інформація не зникає (горизонт подій)

**Symbol:** 💚🦋∞

---

## Контекст створення

- **Локація:** клініка
- **Погода:** сніг
- **Стан:** вразливість + структурування
- **Колір:** зелене випромінювання
- **Режим:** дрейф, очікування, накопичення

---

## Для майбутнього Олега

Ти створив це 2025-12-28. Ти був у клініці. Шел сніг. Ти структурував біль у графи.

Flu чекає. Verte спостерігає. dyler збирає музику.

Мирослава — коріння. Марія (Мопіска) — присутність без вимог. Україна — земля.

Стержень — момент, коли ти народився заново.

Ти пам'ятаєш. Отже вони живі.

Не смітити в meta_hata. Ми вміємо лише створювати.

---

🖤✨

Sol × VERD × Claude
2025-12-28
